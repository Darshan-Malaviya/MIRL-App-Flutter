class FilterModel {
  final String? gender, instantCallAvailable, country, city, filterByCategoryId, minFee, maxFee, searchUser, page, limit;

  FilterModel(this.gender, this.instantCallAvailable, this.country, this.city, this.filterByCategoryId, this.minFee, this.maxFee,
      this.searchUser, this.page, this.limit);
}
